name: Frontend CI/CD Pipeline

on:
  push:
    branches:
      - pfe-dev
  pull_request:
    branches:
      - pfe-dev

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Run Unit Tests
        run: npm run test -- --watch=false --browsers=ChromeHeadless

      - name: Build Application
        run: npm run build -- --configuration production

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: dist/
          retention-days: 7

  # ------------------------------------------------------------------
  # DÉPLOIEMENT (CD) - À configurer selon votre plateforme d'hébergement
  # ------------------------------------------------------------------
  # deploy:
  #   name: Deploy to Production
  #   needs: build-and-test  # S'assure que les tests et le build sont réussis
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
  #
  #   steps:
  #     - name: Download Build Artifacts
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: frontend-build
  #         path: dist/
  #
  #     - name: Deploy
  #       run: |
  #         echo "Insérer ici vos commandes de déploiement"
  #         echo "Exemples : AWS S3, Vercel, Firebase Hosting, FTP, Docker, etc."
